version: 1.0
runtime: nodejs18
build:
commands:
- corepack enable
- pnpm install --frozen-lockfile || npm ci
- pnpm prisma generate || npx prisma generate
- pnpm build || npm run build
run:
command: pnpm start -p 3000 || npm run start
network:
port: 3000